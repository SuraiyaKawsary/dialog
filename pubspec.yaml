name: theree_in_one_complete_app
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  flutter_app_name: ^0.1.0
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/logo.png"
  min_sdk_android: 23

flutter_app_name:
  name: "QCP"

flutter:
  uses-material-design: true
  assets:
    - assets/icons/wlc.png
